android.enableJetifier=true
android.useAndroidX=true
org.gradle.jvmargs=-Xmx8192m

# Versioning
VERSION_NAME=1.9.1
VERSION_CODE=23

# Kotlin/resource namespace, kept as upstream so the sources stay diffable against it
APP_ID=org.fossify.messages

# Installed package id. Distinct from upstream so this fork is its own app.
APPLICATION_ID=org.levinium.messages
